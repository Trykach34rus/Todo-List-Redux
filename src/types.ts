export enum Theme {
	light = 'light',
	dark = 'dark',
}
export type Todo = {
	id: number
	title: string
	complete: boolean
}
export enum FilterT {
	all = 'All',
	complete = 'Complete',
	incomplete = 'Incomplete',
}
